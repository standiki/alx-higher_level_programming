#!/usr/bin/python3
# function that prints a string in uppercase

def uppercase(str):
    for ch in str:
        if ord(ch) >= 97 and ord(c) < 123:
            c = chr(ord(c) - 32)

        print("{:s}".format(c))

    print("")
